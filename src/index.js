import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import PropertyManagementApp from './PropertyManagementApp';

ReactDOM.render(
  <React.StrictMode>
    <PropertyManagementApp />
  </React.StrictMode>,
  document.getElementById('root')
);